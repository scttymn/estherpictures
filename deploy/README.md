# Legacy VM deploy (retired)

Production runs on **Coolify** (`192.168.0.55`). Auto-deploy is via Forgejo
webhook → Coolify on push to `main`.

The scripts and unit file in this directory were for the old systemd app VM
and are kept only as historical reference.
